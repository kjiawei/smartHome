// this file added by yongming.li for node data


#include <string.h>
#include <stdio.h>
#include <stdlib.h>

#if 0
char * strcpy(char *dest, char const *src)
{
	char *tmp = dest;

	while((*dest++ = *src++) != '\0')
		;
	return tmp;
}
#endif 

